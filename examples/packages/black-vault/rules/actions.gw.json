{
  "actions": [
    {
      "id": "alarm.raise",
      "version": 1,
      "inputs": {
        "type": "object",
        "properties": {
          "actorId": {
            "type": "string"
          }
        },
        "required": [
          "actorId"
        ],
        "additionalProperties": false
      },
      "operations": [
        {
          "op": "actor.data.patch",
          "actorId": "$input.actorId",
          "patch": {
            "blackVault.alarm": "RAISED",
            "blackVault.securityResponse": "ACTIVE"
          }
        }
      ],
      "idempotency": "IDEMPOTENT",
      "semantics": [
        "black-vault.alarm.raise"
      ]
    },
    {
      "id": "alarm.suppress",
      "version": 1,
      "inputs": {
        "type": "object",
        "properties": {
          "actorId": {
            "type": "string"
          }
        },
        "required": [
          "actorId"
        ],
        "additionalProperties": false
      },
      "operations": [
        {
          "op": "actor.data.patch",
          "actorId": "$input.actorId",
          "patch": {
            "blackVault.alarm": "SUPPRESSED",
            "blackVault.securityResponse": "JAMMED"
          }
        }
      ],
      "idempotency": "IDEMPOTENT",
      "semantics": [
        "black-vault.alarm.suppress"
      ]
    },
    {
      "id": "vault.unlock",
      "version": 1,
      "inputs": {
        "type": "object",
        "properties": {
          "actorId": {
            "type": "string"
          }
        },
        "required": [
          "actorId"
        ],
        "additionalProperties": false
      },
      "operations": [
        {
          "op": "actor.data.patch",
          "actorId": "$input.actorId",
          "patch": {
            "blackVault.vaultLock": "OPEN"
          }
        }
      ],
      "idempotency": "IDEMPOTENT",
      "semantics": [
        "black-vault.vault.unlock"
      ]
    },
    {
      "id": "vault.trace",
      "version": 1,
      "inputs": {
        "type": "object",
        "properties": {
          "actorId": {
            "type": "string"
          }
        },
        "required": [
          "actorId"
        ],
        "additionalProperties": false
      },
      "operations": [
        {
          "op": "actor.data.patch",
          "actorId": "$input.actorId",
          "patch": {
            "blackVault.trace": "DETECTED",
            "blackVault.securityResponse": "ACTIVE"
          }
        }
      ],
      "idempotency": "IDEMPOTENT",
      "semantics": [
        "black-vault.vault.trace"
      ]
    },
    {
      "id": "terminal.slot-card",
      "version": 1,
      "inputs": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      },
      "operations": [
        {
          "op": "cards.placement.place",
          "card": "SOURCE_CARD",
          "boardZone": "TARGET_SCENE_OBJECT",
          "position": "DROP_WORLD_POSITION"
        }
      ],
      "idempotency": "IDEMPOTENT",
      "semantics": [
        "black-vault.terminal.slot-card"
      ]
    },
    {
      "id": "scanner.engage",
      "version": 1,
      "inputs": {
        "type": "object",
        "properties": {
          "actorId": {
            "type": "string"
          }
        },
        "required": [
          "actorId"
        ],
        "additionalProperties": false
      },
      "operations": [
        {
          "op": "actor.data.patch",
          "actorId": "$input.actorId",
          "patch": {
            "blackVault.scanner": "ACTIVE"
          }
        }
      ],
      "idempotency": "IDEMPOTENT",
      "semantics": [
        "black-vault.scanner.engage"
      ]
    }
  ]
}
